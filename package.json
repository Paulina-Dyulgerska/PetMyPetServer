{
  "name": "pet-my-pet-server",
  "version": "1.0.0",
  "description": "json-server",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start-server": "json-server --watch db.json --port 5000"
  },
  "author": "Paulina Dyulgerska",
  "license": "MIT",
  "dependencies": {
    "json-server": "^0.16.3"
  }
}
